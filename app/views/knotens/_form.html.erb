<%= form_for(@knoten) do |f| %>
  <% if @knoten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@knoten.errors.count, "error") %> prohibited this knoten from being saved:</h2>

      <ul>
      <% @knoten.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :knoten1_id %><br />
    <%= f.text_field :knoten1_id %>
  </div>
  <div class="field">
    <%= f.label :knoten2_id %><br />
    <%= f.text_field :knoten2_id %>
  </div>
  <div class="field">
    <%= f.label :kantenlaenge %><br />
    <%= f.text_field :kantenlaenge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
